============
Installation
============

At the command line::

    $ pip install networking-ibm

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv networking-ibm
    $ pip install networking-ibm
